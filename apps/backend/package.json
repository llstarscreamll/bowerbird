{
  "name": "@bowerbird/backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "[ -f ../../.env ] && set -a && . ../../.env && set +a; exec air -c .air.toml",
    "dev:relay": "[ -f ../../.env ] && set -a && . ../../.env && set +a; exec air -c .air.worker-relay.toml",
    "dev:events-consumer": "[ -f ../../.env ] && set -a && . ../../.env && set +a; exec air -c .air.worker-events.toml",
    "dev:jobs-consumer": "[ -f ../../.env ] && set -a && . ../../.env && set +a; exec air -c .air.worker-jobs.toml",
    "dev:scheduler": "[ -f ../../.env ] && set -a && . ../../.env && set +a; exec air -c .air.worker-scheduler.toml",
    "seed": "set -a && . ../../.env && set +a && go run ./cmd/onprem/seed",
    "build": "mkdir -p bin && go build -o bin/api ./cmd/onprem/api && go build -o bin/relay ./cmd/onprem/relay && go build -o bin/events-consumer ./cmd/onprem/events-consumer && go build -o bin/jobs-consumer ./cmd/onprem/jobs-consumer && go build -o bin/scheduler ./cmd/onprem/scheduler && go build -o bin/lambda-http ./cmd/aws/lambda/http && go build -o bin/lambda-sqs ./cmd/aws/lambda/sqs && go build -o bin/lambda-eventbridge ./cmd/aws/lambda/eventbridge && go build -o bin/lambda-outbox-relay ./cmd/aws/lambda/outbox-relay && go build -o bin/lambda-scheduler ./cmd/aws/lambda/scheduler && go build -o bin/migrate ./cmd/onprem/migrate && go build -o bin/seed ./cmd/onprem/seed",
    "migrate:controlplane": "set -a && . ../../.env && set +a && ./bin/migrate -target controlplane",
    "migrate:tenants": "set -a && . ../../.env && set +a && ./bin/migrate -target tenants",
    "migrate:all": "set -a && . ../../.env && set +a && go run ./cmd/onprem/migrate -target all",
    "test": "[ -f ../../.env ] && set -a && . ../../.env && set +a; go test ./...",
    "lint": "go vet ./..."
  }
}
